/**
 * @deprecated Import from `@/lib/conversation/control-metadata` instead.
 * Barrel re-exports kept so older import paths keep working.
 */
export {
  mergeConversationControl,
  defaultAiLeadControl,
  isConversationHumanControlled,
  resolveConversationHandlingMode,
  getHumanAiControlLabel,
  type ConversationHandlingMode,
} from "./control-metadata";
