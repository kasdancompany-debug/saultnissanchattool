/**
 * Meta webhook signature verification (X-Hub-Signature-256) — scaffold.
 * @returns `false` until wired to app secrets + crypto.
 */
export function validateMetaWebhookSignaturePlaceholder(): boolean {
  return false;
}
