# Meta integrations (scaffold)

Placeholder modules for Messenger and Instagram webhook **parse** / **validate** helpers. Live adapters live in `src/server/inbox/adapters/` (`meta-messenger.adapter.ts`, `meta-instagram.adapter.ts`) and call **`applyInboundMessage`** once payloads are normalized.
